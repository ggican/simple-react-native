import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

const FormInputStyle = StyleSheet.create({
  scrollView: {
    backgroundColor: Colors.lighter,
  },
});

export default FormInputStyle;
